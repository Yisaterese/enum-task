import { ProgramImageList } from '../utils/list' ;
import Image from 'next/image';

const ProgramPages = () => {
    return (
        <div className={''}>
            {

            }
        </div>
    );
}

export default ProgramPages;